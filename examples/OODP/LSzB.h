/** This version has some features not shown in the OODP paper.**/
#import "niqlow"

enum{Tmax=40,Ng=2}
class LSzB : OneDimensionalChoice {
   static decl g, m, L, eps, beta, gam, RV, data;

				EUtility();
    			Uz(z);
				Utility();
    static      Earn();
	static		Home();
    static      Build();
    static 		Run();	
    }
