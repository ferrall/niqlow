#import "BobsChoiceB"

class BobsChoiceC : BobsChoiceB {
        static decl YAcc;
        static Build();
        static Create();
                FeasibleActions();
                // Utility();   Can use B's utility. uncomment C should get a new one
        }
