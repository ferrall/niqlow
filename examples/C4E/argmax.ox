#include <oxstd.h>

/** Return the row indices that maximize the vector of utilities.
@param u nx1 vector
@return px1 vector 
**/
argmax(u) {
  //hint combine maxc() and vecindex() and the element-by-element comparison .==
  return ??;
  }
