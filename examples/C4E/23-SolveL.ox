#include <oxstd.h>

SolveL(A,b,ax) {
	 decl n, j, S;
	 ax[0]  = constant(.NaN,b); 	  //initialize x* 		
	 for(n=0;n < rows(A);++n) {
         // put code here to solve ax[0][n]
		}
	}

main() {
	// Put calls to your function to test it

	}